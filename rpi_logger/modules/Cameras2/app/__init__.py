"""Cameras2 app-layer package specification (no runtime code yet).

Holds the supervisor binding to the stub (codex) stack, model/controller/view, GUI helpers, services, and shared media utilities used by the GUI-facing layer. Assumes integration with `vmc` components (StubCodexSupervisor/Model/Controller) for config persistence, status reporting, and GUI wiring.
"""
